#+title: Development Status & Continuation Handoff
#+TAGS: me ai aiAndMe

# Provenance (installed once by aiActivity.cs --- not regenerated):
#   Date:      2026-08-12T16:55:56-07:00
#   User:      bystar
#   Templates: /bisos/apps/defaults/ai-templates
#   Command:   /bisos/venv/py3/devbin/aiActivity.cs -i initiate --activity=bisos-pip

####+BEGIN: b:ai:file/particulars
* AI Working Context
  Working Directory: /bisos/git/auth/bxRepos/bisos-pip/fileObj
  File: /bisos/git/auth/bxRepos/bisos-pip/fileObj/AI-DevStatus.org
  Activity: bisos-pip
  Companion Docs: AI-WORKFLOW.org, AI-Activity.org, AI-DevStatus.org, README.org
####+END:

This file is a working-state handoff so development can continue on another
machine (which has this repo but NOT the developer's =~=). It captures the
architecture, what works, what is untested and the design decisions made so far.

This file is maintained by Claude. Updates are typically triggered by the
=/bx-update-status= slash command, after major interactions, or before a release.

Note to a fresh Claude: this file is intentionally sparse at project start.
Fill in the sections below when asked to update, following the structure provided.

* Current State

Both Stage 1 and Stage 2 of =bisos.fileObj= are code-complete and pass
=py3/tests/verify.sh= (32 assertions, all PASS). The package is
pip-installed editable in =/bisos/venv/py3/bisos3/= (via
=pypiProc.sh -i pkgReInstall=).

- Package version: =0.1= (unreleased; not on PyPI).
- Stage 1 (walker library + branch =.spcs= framework): 10 TODOs DONE,
  2 open (Blee panel scaffolding #B, PyPI 0.1 release #B).
- Stage 2 (walking with a command + typed leaf examples + markerless
  walks): 6 A-priority deliverables DONE, 1 open (exit-code propagation
  upstream fix in =bisos.b.cs=, #C).
- End-to-end validated against a real consumer: *all six* branches
  under =bro_dockerfiles= (deb12/{confined,privileged,rootless-sysd}
  and deb13/{confined,privileged,rootless-sysd}) now carry
  =ftoBranchProc.spcs= that pulls =walkExamples()= and
  =leafProcessorNames()= from =containerProc_seedInfo=. Five of the
  six use the new *markerless* pattern (Deliverable 5): no
  =_tree_=/=_treeProc_= files at intermediate =vnc/=/=xfce/= dirs and
  none at the leaf itself; leaf detection is definitional via the
  presence of =dockerProc.spcs=/=podmanProc.spcs=. The sixth
  (=deb12/confined/=) intentionally retains legacy explicit markers as
  a regression-canary for the explicit-marker code path. Interactive
  =-i fto_treeList= and =-i fto_forwardToLeaves= verified at every
  branch; VM-specific behavior (docker/podman actually running) still
  untested.

* Architecture

Six hand-authored files, all in COMEEGA/dblock format:

** =py3/bisos/fileObj/= (Python package)

- =fto.py= — pure walker library (=cs-lib= classification).
  Provides =FileTreeItem= enum (=Branch=, =Leaf=, =AuxBranch=,
  =AuxLeaf=, =Ignore=), =FILE_TreeObject= class (node create/read/predicate
  methods), and walker functions (=branchPredicate=, =leafPredicate=,
  =effectiveBranches=, =effectiveLeaves=, =treeRecurse=, =WalkResult=).
  Reads legacy bash =node= / =auxNode= for backward compatibility; writes
  =branch= / =auxBranch= going forward.
  Stage 2 Deliverable 5: =FILE_TreeObject.nodeType()= and =.treeProc()=
  accept an optional =leafProcessors= parameter that lets the walker
  detect leaves *definitionally* (by presence of a domain =.spcs= file)
  and default unmarked directories to =AuxBranch=. =treeRecurse= reads
  =ftoBranchSeedInfo.leafProcessors= from the singleton if the caller
  doesn't pass one explicitly.

- =fto_csu.py= — CS-Lib exposing =fto_*= Cmnds (=cs-lib= classification).
  Three Cmnd groups:
  - *Introspection:* =fto_nodeInfo=, =fto_branchPredicate=,
    =fto_leafPredicate=, =fto_effectiveBranches=, =fto_effectiveLeaves=,
    =fto_treeList=.
  - *Creation:* =fto_branchCreate=, =fto_leafCreate=,
    =fto_auxBranchCreate=, =fto_auxLeafCreate=, =fto_ignoreCreate=.
  - *Forwarding (Stage 2):* =fto_forwardToLeaves= (Mode 1 --- forwards
    =-i <cmndName>= to each leaf's =_treeProc_=), =fto_walkRunExternal=
    (Mode 2 --- runs an arbitrary external cmnd at every visited node,
    with per-node =shutil.which=/=<nodeDir>/cmnd= "perhapsRun"
    resolution).
  Plus =examples_csu= (Cmnd) and =commonParamsSpecify=
  (=path=, =treeProc=, =objectType=, =cmndName= parameters). Every Cmnd
  authored via =b:py3:cs:cmnd/classHead= dblock, fully expanded.
  =examples_csu= now renders leaf-provided walk examples from
  =ftoBranchSeedInfo.leafExamples= as proper =cs.examples.cmndEnter=
  entries (see Deliverable 4 under "Key Design Decisions").

- =ftoBranch_seedInfo.py= — SeedInfo (=cs-u= classification).
  =BranchParams= dataclass; =paramsFromPlantPath()= (trivial-path case:
  branch is =resolve().parent= of the planted =.spcs=);
  =WalkExampleSpec= dataclass (Stage 2: =cmndName=, =pars=, =args=,
  =comment=, =mode=, =tags=); =FtoBranchSeedInfo= singleton with fields:
  =branchesList=, =leavesList=, =branchesExcludes=, =leavesExcludes=,
  =branchesOrdered=, =leavesOrdered=, =examplesFuncsList= (callable
  escape-hatch hook), =leafExamples= (typed data list from leaf-side
  seed), =leafProcessors= (Deliverable 5: list of filenames whose
  presence marks a directory as a leaf). =setup()= populates the
  singleton from a =.spcs= call.

- =ftoBranch_seed.py= — atexit registrar (minimal). Registers
  =plantWithWhich('ftoBranch-seed.cs')= via =@atexit.register=.

** =py3/bin/= (executables)

- =ftoBranch-seed.cs= — seed CSXU (=cs-mu= classification).
  Full COMEEGA header chain. Imports =csxuFps_csu=, =fto_csu=,
  =ftoBranch_seedInfo=, and =plantedCsu= when planted. Uses
  =csuListProc= dblock. =examples= Cmnd calls
  =fto_csu.examples_csu().pyCmnd()=, then iterates
  =examplesFuncsList= (callable hook, wrapped in =try/except=), then
  runs =plantedCsuExamplesRun()= if a plant is active. Uses
  =ignoreUnknownParams=True=.

- =ftoBranchProc.spcs= — thin stub template (~26 lines).
  Imports =ftoBranch_seed= for the atexit hook. Optional
  =ftoBranch_seedInfo.setup(...)= call commented as an example. This
  file ships in =py3/bin/= as a template; consumers copy it to a
  branch position and customize (e.g. adding
  =setup(leafExamples=containerProc_seedInfo.walkExamples())=).

** =py3/tests/=

- =verify.sh= — smoke tests, *30 PASS 0 FAIL*. Three main sections:
  1. *Library-level* (22 assertions): symbol presence, =FileTreeItem=
     enum, =fromString= (new + bash-compat: =node=/=auxNode=/=ignoreNode=/
     =ignoreLeaf=), predicates, effectiveBranches/effectiveLeaves
     autodiscover, bash =node= backward-compat read, walker traversal
     respecting =ignore= and =auxBranch=, symlink-cycle survival.
  2. *Branch =.spcs= end-to-end (Tests 1--5):* =fto_nodeInfo=,
     =fto_effectiveLeaves=, =fto_forwardToLeaves= (leaf marker
     assertions), =fto_walkRunExternal= (touch markers at branch + 2
     leaves), =fto_walkRunExternal= with nonexistent cmnd (perhapsRun
     skip semantics).
  3. *Custom-.spcs tests (Tests 6--8):* =examplesFuncsList= callable
     hook, =leavesExcludes= override, =leafExamples= typed data rendering.

** =py3/setup.py=, =py3/pyproject.toml=, =py3/_description.org=, =py3/README.rst=

Never hand-edit. All regenerated by =pypiProc.sh=. Rule now documented
in the =bisos-pip-setup-py= skill and mentioned in this repo's
=AI-Activity.org= as part of the general "COMEEGA Authorship" story.

* What Works

Verified by =verify.sh= on 2026-08-14: *32 PASS, 0 FAIL.*

- *Library layer* (Tests: 22 library-level assertions).
- *Branch =.spcs= end-to-end (Stage 1):*
  - =fto_nodeInfo= dispatches locally, reports =_tree_ = Branch=.
  - =fto_effectiveLeaves= lists both leaves.
- *Branch =.spcs= end-to-end (Stage 2):*
  - =fto_forwardToLeaves --cmndName=someVerb= invokes each leaf's
    =_treeProc_ -i someVerb=; leaf markers confirm both leaves ran.
  - =fto_walkRunExternal touch marker.txt= creates a marker at the
    branch + both leaves (3 markers total; auxBranches correctly
    traversed-not-applied).
  - =fto_walkRunExternal <nonexistent-cmnd>= skips all 3 apply sites
    via =perhapsRun= semantics; walk continues.
  - =examplesFuncsList= callable hook renders its chapter in the
    bare-invocation menu.
  - =leavesExcludes= override causes =fto_forwardToLeaves= to skip
    the excluded leaf.
  - =leafExamples= typed data (=WalkExampleSpec= list) renders both
    Mode-1 and Mode-2 chapters with all specs.
- *Markerless walk (Stage 2 Deliverable 5, Test 9):*
  - =fto_treeList= over a scratch tree with =leafProcessors=['leaf.spcs']=
    declared and NO =_tree_=/=_treeProc_= files anywhere correctly
    identifies the leaf definitionally (via presence of =leaf.spcs=).
  - =fto_forwardToLeaves= against the same markerless scratch tree
    reaches the leaf and invokes its =leaf.spcs= (marker file
    confirms invocation).

Additionally verified interactively (against real BPO tree, complementary
to the scratch-tree verify.sh coverage):

- *=bro_dockerfiles/debian/12/confined/= tree walk:* 4-level chain
  (branch + 2 auxBranches + leaf) traverses cleanly via
  =fto_treeList=; the branch's =ftoBranchProc.spcs= now imports
  =containerProc_seedInfo.walkExamples()= and renders 13 leaf-provided
  walk examples in its menu. This branch retains legacy explicit
  =_tree_=/=_treeProc_= markers as a regression canary for the
  explicit-marker code path.

- *All six =bro_dockerfiles= branches planted (2026-08-14):*
  deb12/{confined,privileged,rootless-sysd} and
  deb13/{confined,privileged,rootless-sysd}. Five of six use the
  Deliverable 5 *markerless* pattern --- zero =_tree_=/=_treeProc_=
  files at intermediate =vnc/=/=xfce/= dirs and none at the leaf
  itself. Leaves are detected definitionally via
  =containerProc_seedInfo.leafProcessorNames()= returning
  =['dockerProc.spcs', 'podmanProc.spcs']=. Interactive =fto_treeList=
  and =fto_forwardToLeaves --cmndName=containerProc_instancePs= verified
  at each branch; leaf-side seed correctly refuses with
  =RuntimeError: /.dockerenv exists= at each leaf.

* What Is Untested

- *Exit-code propagation from leaf =.spcs=:* the =bisos.b.cs.main.g_csMain=
  framework exits 0 even when the invoked Cmnd raises. Both
  =fto_forwardToLeaves= and =fto_walkRunExternal= therefore
  under-report failures in =WalkResult.failed=. Fix is a =bisos.b=
  concern; captured as Stage 2 #C TODO.
- *Real docker/podman execution:* all Stage 2 tests inside this
  container correctly hit the =_refuseIfInContainer()= gate in
  =bisos.dockerProc=. VM validation of actual builds/runs is pending.
- *Blee panel:* =py3/panels/bisos.fileObj/_nodeBase_/fullUsagePanel-en.org=
  still contains a stale copy of the =bisos.gitist= panel. Stage 1 #B TODO.
- *PyPI release:* never uploaded. Stage 1 #B TODO.
(No remaining gaps specific to Deliverable 5 --- verify.sh Test 9
added 2026-08-14 covers the markerless-walk path in a scratch tree;
full suite is now 32 PASS, 0 FAIL.)

* Key Design Decisions

**Vocabulary: branch/leaf, not node/leaf.** The bash reference at
=/bisos/core/bsip/bin/lcnObjectTree.libSh= uses =node= to mean what a
proper tree data structure calls a =branch=. Python port writes
=branch= going forward; reads bash-written =node= for compatibility.
No first-class =root= concept — root is wherever the user chose to
start walking.

**Explicit forwarding Cmnd, not transparent dispatch (2026-08-13).**
The =ftoBranchProc.spcs= is a pure pass-through to the seed
(=dockerProc.spcs= pattern). It does NOT inspect the =-i <cmnd>= name
and forward unknown Cmnds to leaves. Instead, forwarding will be an
explicit =fto_forwardToLeaves= Cmnd (Stage 2). Rationale: keeps the
.spcs a genuine thin stub matching the bisos-pip convention.

**Branch =.spcs= uses trivial-path resolution.** The plant path itself
IS the parameter — no anchor-segment parsing like
=containerProc_seedInfo.paramsFromPlantPath()= does with =debian/=. For
=ftoBranchProc.spcs=, =branchPath = Path(plantOfThisSeed).resolve().parent=.
The skill =spread-seeded-cs-commands= should be updated to cover this
"trivial path" case; noted for future work.

**COMEEGA everywhere.** Every hand-authored source file carries the
full COMEEGA header dblock chain (=dblockControls=, =proclamations=,
=particulars=, =particulars-csInfo=, =orgTopControls=, =workbench=,
=framework/imports=, etc.). Every =cs.Cmnd= class is authored via
=b:py3:cs:cmnd/classHead= dblock, fully expanded (matches Blee's
regenerated form). This is a correction from the previous Claude's
draft, which had hand-authored Cmnd classes without dblock structure.
Rule now captured in the new =comeega-authorship= skill and in the
=bisos-pip-setup-py= skill's "Mandatory COMEEGA dblock structure"
section.

**Never hand-edit setup.py / pyproject.toml / _description.org /
README.rst.** All four are regenerated by =pypiProc.sh=. Rule now
captured in the =bisos-pip-setup-py= skill frontmatter and top section.
=README.org= at the repo root is the only authored README.

**bisos.fileObj as an umbrella package.** Charter is
filesystem-directory-backed data structures. =fto= is the first module;
future modules (=fp=, =fv= migrating from =bisos.b=) will join.
See AI-WorkPlan.org "Stage N (Reserved)" and README.org Charter section.

**Two orthogonal walk modes (Stage 2, 2026-08-13).** Following the
bash reference's =treeRecurse= vs =ftoWalkRunCmnd= distinction:
- *Mode 1* (=fto_forwardToLeaves --cmndName=X=): at each effective
  leaf, invoke =<leafDir>/<_treeProc_> -i X=. The verb is a Cmnd name
  the leaf's own dispatcher understands.
- *Mode 2* (=fto_walkRunExternal <cmnd> [args...]=): at each visited
  node, invoke an external cmnd. Node's =_treeProc_= is used only as
  a marker (leaf vs auxLeaf) --- the external cmnd supplies its own
  executable. Matches bash =perhapsRun= semantics: skip silently if
  the cmnd isn't resolvable from the node dir or PATH.
- No unified "smart" forwarder --- callers pick the mode explicitly.

**Leaf-seed-owned walk examples (Stage 2, Deliverable 4).** The
source of truth for "which leaf verbs make sense to walk with" is the
*leaf-side seed*, not the branch =.spcs=. Concretely,
=bisos.dockerProc.containerProc_seedInfo.walkExamples()= returns a list
of =WalkExampleSpec= entries. The branch =.spcs= imports that function
and passes the result to =ftoBranch_seedInfo.setup(leafExamples=...)=.
Adding a new =containerProc_*= Cmnd requires updating =walkExamples()=
once; every branch under =bro_dockerfiles= picks it up on next menu
render. Design decisions locked: D1(c) (branch =.spcs= does the import;
no subprocess bridge yet), D2(a) (=WalkExampleSpec= dataclass lives in
=bisos.fileObj=).

**Markerless walks --- leaf-processor detection + auxBranch default
(Stage 2, Deliverable 5, 2026-08-14).** Two ideas combined:
- *Idea 1 --- leaf-processor definition.* A leaf IS a directory where
  tree processing ends. The presence of the leaf's planted =.spcs=
  file IS the definition; no =_tree_=leaf= marker needed. The domain
  seed exposes =leafProcessorNames() → list[str]= (symmetric with
  =walkExamples()=); the branch =.spcs= passes it through
  =ftoBranch_seedInfo.setup(leafProcessors=...)=.
- *Idea 2 --- auxBranch default for unmarked dirs.* When the walker
  enters a directory that has no =_tree_= marker AND no matching
  leaf-processor, treat it as auxBranch (traverse but don't apply).
  Intermediate pass-through directories (=vnc/=, =xfce/=) need no
  markers.
Classification precedence: explicit =_tree_= marker → leaf-processor
match → auxBranch default → =None= (legacy, when =leafProcessors= is
not passed). =_tree_=auxLeaf= remains the only marker that meaningfully
modifies leaf behavior ("this IS a leaf but skip application").
Walker reads =leafProcessors= from the singleton lazily; recursion
threads the resolved value forward for a single singleton read per
walk. Result: adopting a BPO tree drops from "8 marker files + 3
branch =.spcs= copies at intermediates" to "1 branch =.spcs= + 0
marker files." Five of six =bro_dockerfiles= branches use the new
markerless pattern; =deb12/confined/= retains legacy markers as a
regression canary for the explicit-marker code path.

*Deliverable 3 reclassified (2026-08-13).* An earlier design
(Deliverable 3 in AI-WorkPlan) shipped =examplesFuncsList= --- an
arbitrary-callable hook that lets a branch =.spcs= emit any menu
content via =cs.examples.execInsert()=. It was initially the primary
mechanism for domain-specific examples. Deliverable 4 corrects that:
the callable hook is *superseded in role* (typed =leafExamples= is
the primary path) but *retained in code* (Test 6 in verify.sh still
exercises it) as an escape hatch for cases where typed data can't
express what the branch wants (arbitrary chapter headings, prose
blurbs, custom entries).

**=_applyCommand= precedence: planted-copy wins over PATH
(2026-08-13).** Bug discovered during first T2 test with
=bro_dockerfiles/debian/12/confined/=: the walker resolved a leaf's
=_treeProc_= value via =shutil.which()= *first*, then fell back to
the node dir. Wrong direction --- "spread planted" contract is that
each leaf's own copy is authoritative. Fixed in =fto.py:436-448=.

**=_resolveList= excludes must apply to autodiscover too
(2026-08-13).** Second bug found during Test 7: excludes were only
honored when =listed= was also given; pure-excludes-with-autodiscover
was silently ignored. Fixed to apply excludes uniformly to whichever
candidate set (listed or autodiscover) is in play.

**Exit-code propagation limitation (known, deferred).**
=bisos.b.cs.main.g_csMain= exits 0 even when a Cmnd raises, which
means =WalkResult.failed= under-reports leaf failures. Fixing this is
a =bisos.b= concern (upstream); tracked as Stage 2 #C TODO.

* Cross-Repo Integration: =bro_dockerfiles= (2026-08-13 → 2026-08-14)

The first real consumer of =bisos.fileObj= is =bro_dockerfiles=. As of
2026-08-14, *all six* branches are planted: deb12/{confined,privileged,
rootless-sysd} and deb13/{confined,privileged,rootless-sysd}.

Two planting styles coexist by design:

** =debian/12/confined/= --- explicit-marker style (legacy pilot, kept as canary)

Marker files at all four levels of the branch → auxBranch → auxBranch →
leaf chain:

| Path                                              | =_tree_=    | =_treeProc_=         | .spcs file      |
|---------------------------------------------------+-------------+----------------------+-----------------|
| =debian/12/confined/=                             | =branch=    | =ftoBranchProc.spcs= | branch =.spcs=  |
| =debian/12/confined/vnc/=                         | =auxBranch= | =ftoBranchProc.spcs= | copy of stub    |
| =debian/12/confined/vnc/xfce/=                    | =auxBranch= | =ftoBranchProc.spcs= | copy of stub    |
| =debian/12/confined/vnc/xfce/bisos_deb12-fresh/=  | =leaf=      | =dockerProc.spcs=    | (already there) |

Kept in this shape *deliberately* as a regression canary for the
explicit-marker code path (per user direction, 2026-08-14).

** The other five branches --- markerless style (Deliverable 5)

Zero marker files at intermediate =vnc/=/=xfce/= or at the leaf itself.
Only the branch's own =ftoBranchProc.spcs= file lives at the branch dir,
and the leaf's own domain =.spcs= file (=dockerProc.spcs= or
=podmanProc.spcs=) at the leaf dir. Leaves are detected *definitionally*
via =leafProcessors=.

** Branch =.spcs= (identical across all six branches)

: from bisos.fileObj import ftoBranch_seed  # atexit
: from bisos.fileObj import ftoBranch_seedInfo
: from bisos.dockerProc import containerProc_seedInfo
:
: ftoBranch_seedInfo.setup(
:     leafExamples=containerProc_seedInfo.walkExamples(),
:     leafProcessors=containerProc_seedInfo.leafProcessorNames(),
: )

This is the *canonical example* of the composed Deliverable-4
(=walkExamples()=) + Deliverable-5 (=leafProcessorNames()=) pattern.
Adding new =containerProc_*= Cmnds or new leaf =.spcs= filenames
updates the source of truth in =containerProc_seedInfo= *once* --- all
six branches (and any future ones) pick it up automatically.

The old bash-era markers under leaf =_nodeBase_/= (containing legacy
=node= vocabulary, pointing at =ftoProc.sh=) are left alone. Bash and
PyCS FTO systems coexist by not sharing =_tree_=/=_treeProc_= files.

* Skills Updated in This Repo (2026-08-13)

- =bisos-pip-setup-py= — frontmatter and top section rewritten to
  cover all four regenerated files (=setup.py=, =pyproject.toml=,
  =_description.org=, =README.rst=), not just =setup.py=.

- =comeega-authorship= — NEW skill. Covers the COMEEGA authorship
  model cross-language (Python, elisp, bash, LaTeX, HTML stub).
  Includes universal "never hand-edit inside dblock bodies" rule,
  per-language syntax table, and the BISOS Python file header dblock
  chain in order.

* Template Updated (Affects Every bisos-pip Repo)

=/bisos/apps/defaults/ai-templates/bisos-pip/AI-Activity.org=:

- Added TOC entry =COMEEGA Authorship=.
- Added =COMEEGA Authorship= section between =Programming Conventions=
  and =Python Library Preferences=. Content: definition, universal
  dblock rule, header dblock chain, canonical reference file + URL,
  pointers to related skills.
- Added =comeega-authorship= row to the "Available Skills" table.

Existing bisos-pip repos initiated before this change won't see the
new section until =aiActivity.cs -i initiate= is re-run (or the
symlink is refreshed). This repo (=fileObj=) uses the symlink and
picks up the change immediately.

# Local Variables:
# eval: (setq-local toc-org-max-depth 4)
# End:
